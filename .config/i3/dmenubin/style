# Colors (accent, dark, light, urgent)
neutral="#8e8e93"
dark="#3a3a3c"
light="#f2f2f7"
accent="#ff9f0a"

# Style
lines="50"
font="FontAwesome-15"

# Theme
theme="-l $lines -nf $neutral -nb $dark -sb $accent -sf $light -fn $font"
